import React from 'react'

const CustomSelect = ({children, data}) => {
  return (
    <div className='customSelect_wrapper'>
      <div className="selected"></div>
      {data.map(item => {
        
      })}
    </div>
  )
}

export default CustomSelect